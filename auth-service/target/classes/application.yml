server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://localhost:3306/stockpro_auth?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "#Bhulgaya123"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    show-sql: true
  flyway:
    enabled: true
    baseline-on-migrate: true

jwt:
  # This should be at least 256 bits (32 bytes) long
  secret: c3RvY2twcm8tc3VwZXItc2VjcmV0LWtleS1mb3ItYXV0aGVudGljYXRpb24tdG9rZW4=
  # 8 hours in milliseconds (8 * 60 * 60 * 1000)
  expiration: 28800000

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
